# application.yml是用户级别的配置
# bootstrap.yml是系统级别的配置

spring:
    cloud:
        config:
            name: config-client # 读取config-client.yml文件 ，不需要后缀 （从git上读取的资源名称）
            profile: dev # 在这里读取了端口号是8201
            label: master # 读取master分支
            uri: http://localhost:3344





