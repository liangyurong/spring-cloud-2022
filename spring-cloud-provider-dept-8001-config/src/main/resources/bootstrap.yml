spring:
    cloud:
        config:
            name: config-dept # 读取config-dept.yml文件 ，不需要后缀 （从git上读取的资源名称）
            profile: dev # 在这里读取了端口号是8201
            label: master # 读取master分支
            uri: http://localhost:3344